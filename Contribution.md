# How to build

```shell

# First time (https://chocolatey.org/)
choco install nodejs.install # if not already installed
choco install fake # tested with fake 5.2.0

# create new version
fake run build.fsx

```
